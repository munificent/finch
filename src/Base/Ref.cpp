#include <memory>

#include "Macros.h"
#include "Ref.h"

namespace Finch
{
    /*
    template <class T>
    std::ostream& operator<<(std::ostream& cout, const Ref<T> & ref)
    {
        if (ref.IsNull())
        {
            cout << "(null reference)";
        }
        else
        {
            cout << *ref;
        }
        
        return cout;
    }*/
}